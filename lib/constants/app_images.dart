//------------------------------------class for define a the static images that we used in our app
class AppImages {
  static const root = 'assets/images/'; //--the root of images folder path
  static const logo = '${root}logo.jpg';
  static const appLogo = '${root}app_logo.png';
}
